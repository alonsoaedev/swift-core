import Cocoa

//let actors = Set(["Denzel Whasington", "Tom Cruise", "Nicolas Cage", "Samuel L Jackson",])
//print(actors)

var actores = Set<String>()
actores.insert("Denzel Whasington")
actores.insert("Tom Cruise")
actores.insert("Nicolas Cage")
actores.insert("Samuel L Jackson")
print(actores) // without order

//var numbers = Set(1, 2, 3, 4) // the constructor must receive an array
